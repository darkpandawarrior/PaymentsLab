# SmartPay (SMPAY)

- **Region:** unconfirmed (Jugnoo reference branch `f_smpay` doesn't specify)
- **Archetype:** C (hosted checkout), assumed — this is the thinnest-documented gateway in the
  catalog; even its region wasn't confirmed against the Jugnoo indexes this session.
- **Status shipped:** `MOCK_MODE`.
- **Docs:** not located this session.

Catalog entry per the plan's "nothing dropped silently" rule, flagged as the least-verified entry in
the batch. `MOCK_MODE`, generic `HostedWebViewAdapter`.
