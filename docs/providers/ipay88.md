# iPay88

- **Region:** Malaysia
- **Archetype:** A (native SDK, local AAR per Jugnoo's `f_ipay_88` reference) in reality — this demo
  runs it through the generic archetype-C mock checkout path instead (same simplification as
  PhonePe/Worldpay/PayU).
- **Status shipped:** `MOCK_MODE`.
- **Docs:** not located this session — iPay88's Android SDK is typically distributed as a local AAR
  via direct merchant agreement, not a public Maven artifact.
