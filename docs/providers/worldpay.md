# Worldpay

- **Region:** Global
- **Archetype:** A (native SDK) in reality — per Jugnoo's reference (`f_pg_worldpay`); this demo runs
  it through the generic archetype-C mock checkout path instead (same simplification as PhonePe —
  wiring the real `access-checkout-android` SDK + a verified backend order-creation API is future work).
- **Status shipped:** `MOCK_MODE`.
- **Docs:** https://developer.worldpay.com (real API contract not verified against live docs this
  session beyond what `research-notes.md` already captured: `access-checkout-android:2.0.0`).
