# Paytm All-in-One

- **Region:** India
- **Archetype:** A (native SDK) in reality — per general hosted-checkout provider conventions; this demo runs it through the generic archetype-C mock checkout path instead (same
  simplification as PhonePe/Worldpay).
- **Status shipped:** `MOCK_MODE`.
- **Docs:** https://business.paytm.com/docs (real API contract not re-verified against live docs this
  session — `research-notes.md` notes staging MID access is flaky, matching this MOCK_MODE call).
