# Paymark

- **Region:** New Zealand
- **Archetype:** C (hosted checkout) — per general hosted-checkout provider conventions, no
  Android SDK.
- **Status shipped:** `MOCK_MODE`.
- **Docs:** https://www.paymark.co.nz (specific API contract not verified against live docs this
  session).

## Why MOCK_MODE

Sandbox self-serve status and the exact checkout contract weren't confirmed this pass. `MOCK_MODE` +
docs only, generic `HostedWebViewAdapter`.
