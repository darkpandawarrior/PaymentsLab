# MyFatoorah

- **Region:** Kuwait/MENA
- **Archetype:** C (hosted checkout, InvoiceURL-style redirect) — per general hosted-checkout provider conventions, no Android SDK.
- **Status shipped:** `MOCK_MODE`.
- **Docs:** https://docs.myfatoorah.com (redirected from the older readme.io host; specific API
  contract not verified against live docs this session).

## Why MOCK_MODE

Sandbox self-serve status and the exact InvoiceURL-create contract weren't confirmed this pass.
`MOCK_MODE` + docs only, generic `HostedWebViewAdapter`.
