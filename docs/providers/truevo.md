# Truevo

- **Region:** EU/Malta
- **Archetype:** C (hosted checkout) — per general hosted-checkout provider conventions.
- **Status shipped:** `MOCK_MODE`.
- **Docs:** https://truevo.com (API contract not verified against live docs this pass).

Sandbox self-serve status and the exact checkout contract weren't confirmed this pass. `MOCK_MODE` +
docs only, generic `HostedWebViewAdapter`.
