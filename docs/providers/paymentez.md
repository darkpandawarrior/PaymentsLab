# Paymentez

- **Region:** LATAM
- **Archetype:** C (hosted checkout) — per Jugnoo's reference integration (`f_paymentez`).
- **Status shipped:** `MOCK_MODE`.
- **Docs:** https://paymentez.readme.io (API contract not verified against live docs this pass).

Sandbox self-serve status and the exact checkout contract weren't confirmed this pass. `MOCK_MODE` +
docs only, generic `HostedWebViewAdapter`.
