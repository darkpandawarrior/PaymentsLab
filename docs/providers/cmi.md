# CMI

- **Region:** Morocco
- **Archetype:** C (hosted checkout) — per Jugnoo's reference integration (`f_pg_cmi`), no Android SDK.
- **Status shipped:** `MOCK_MODE`.
- **Docs:** https://www.cmi.co.ma (specific API contract not verified against live docs this session).

## Why MOCK_MODE

Sandbox self-serve status and the exact checkout contract weren't confirmed this pass. `MOCK_MODE` +
docs only, generic `HostedWebViewAdapter`.
