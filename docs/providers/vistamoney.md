# VistaMoney

- **Region:** LATAM
- **Archetype:** C (hosted checkout) — per general hosted-checkout provider conventions, no
  Android SDK.
- **Status shipped:** `MOCK_MODE`.
- **Docs:** not located this session — VistaMoney has minimal public English-language developer
  documentation.

## Why MOCK_MODE

Sandbox access and the exact checkout contract are unverified — this catalog entry exists to track
the gateway (per the plan's "nothing dropped silently" rule) with an honest `MOCK_MODE` + docs-TBD
state, generic `HostedWebViewAdapter`.
